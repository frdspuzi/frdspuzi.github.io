# component-lab Handoff Briefing

**To the incoming agent/session:** `component-lab/` is a full Vite + React port of frdspuzi.github.io, and as of 2026-08-12 it **is the live site** — merged into `master`, and GitHub Pages' Source setting switched from "Deploy from a branch" (the old Jekyll auto-build) to "GitHub Actions" (deploys `component-lab/dist` via `.github/workflows/deploy-pages.yml`). The Jekyll source that used to live at the repo root (`_includes/`, `_layouts/`, `assets/styles.scss`, `DESIGN.md`, the root `.ai/` pair) has been deleted — there is no second stack to worry about anymore. Read `component-lab/.ai/architecture.md` first for the persistent rules (tech stack, the component-sourcing rule, the shared accordion system's hard-won invariants), this file for current state.

**Still shared with the old content pipeline, not Jekyll-specific — don't delete these thinking they're leftover Jekyll cruft:** `_data/*.json`, `_posts/*.md` (both imported directly by `component-lab/src`), and `assets/{photography,youtube-thumbnails,medium-images}/` (synced into `component-lab/public/assets/` by `scripts/sync-assets.js` on every `predev`/`prebuild` — never committed there directly). All written by `.github/scripts/*.js` on the same schedule as before.

## Status (2026-09-24 session — gratitude + learning now Claude-only; Gemini jobs removed)

**`ai-daily-content.yml` now runs only the YouTube job.** Its `gratitude` and `learning` jobs (Gemini) were removed because they ran *after* the Claude cloud routines and overwrote them every night: the Claude routines commit at ~22:19 and ~22:3x UTC, and the Actions `[skip ci]` commits followed at ~00:03 UTC. So since 2026-09-21 the live site showed Gemini content most of the day, and the Gemini calls were still being billed. `ai-gratitude.yml` / `ai-learning.yml` remain as manual-only fallbacks (`workflow_dispatch`; they have no schedule of their own). The routines' own commits (no `[skip ci]`) trigger `deploy-pages.yml` via its normal `push` trigger.

**Both routine prompts sync first** (`git fetch origin && git checkout -B master origin/master` before writing), keep their own version if the data file conflicts on rebase, and never skip because the file was "already updated today". Before this, the gratitude routine started on a detached HEAD, did `git checkout master` onto a stale local branch, hit a conflict with the newer Gemini-written file, concluded the day was already covered, discarded its own work, and still reported success (2026-09-24 manual run; re-run published `2efe7cc`). The learning routine's first 15-question run (`1dbcb69`) split 3/3/3/2/2/2 across 6 articles as designed.

**Learning routine** ("AI Learning Trivia (Claude, replaces Gemini)", `trig_015ZtrruwyZfqhTrQMeGrd7U`, 22:30 UTC): reads `_data/medium.json` from the repo instead of fetching. The cloud sandbox blocks outside sites; a 2026-09-23 run failed on `api.rss2json.com` and `medium.com` egress. It now makes **15 questions** spread across all usable articles (`floor(15/N)` each, remainder to the longest), with distinct angles/facts. It's scheduled 30 min after `medium.yml`'s 22:00 refresh. A manual test run with the earlier 5-question prompt produced valid output (correct `correctIndex`, links matching `medium.json`).

## Status (2026-09-24 session — YouTube feed excludes livestreams)

**`fetch_youtube.js` now skips livestreams entirely: upcoming, live, and ended** (site owner's call). Trigger: two AI Engineer Paris streams got curated with no key moments. One was still *scheduled* when curated, so its card summary was written from the title/description of a talk that hadn't happened. Every Gemini enrichment attempt on both 403'd (nothing to watch yet / not yet processed). Detection is `isLivestreamPage(html)`, which checks the watch page for `"isLiveContent":true`. That flag is present on upcoming, live and ended streams alike, and verified with the script's own bare `fetchHtml` request. It's applied to new candidates **before** `evaluateBulk` (so livestreams never cost tokens or win a slot) and to videos already in the feed. A fetch failure or a page missing the field counts as *not* live, so a YouTube hiccup can't empty the feed. Costs one watch-page fetch per candidate plus per feed video (~80 per run, 500ms pacing, roughly +1 min of CI). `_data/youtube.json` was also hand-cleaned of the two streams the same day. Caveat: the feed file is only rewritten on runs that curate at least one new video, so on a day with zero picks, feed pruning waits for the next day with a pick.

## Status (2026-09-23 session — mobile sections auto-maximize after opening)

**On phones (≤767px), opening a main (`groupable`) accordion section now maximizes it once the open animation finishes** — per request: open → animation → maximized view. Order matters (user feedback: it must snap to the top first): open animation → `scrollIntoViewIfMobile` scrolls the section to the top → maximize. `scrollIntoViewIfMobile(id, onScrolled)` now takes a completion callback, fired on `scrollend` (immediately if already at the top; 1s timer fallback for browsers without `scrollend`; synchronously with reduced motion), and `Accordion.tsx` maximizes from it. A first version used a fixed `setTimeout(OPEN_DURATION)`, which fired mid-scroll, because the scroll only starts once the page stops resizing (often ~1.4s with images loading). An `autoMaximizeRef` set in a `useLayoutEffect` declared *before* `useAnimatedDisclosure` gates it: only for a section the visitor opens (not one open at mount) and cleared on close. The maximize itself is instant (no CSS transition exists); a short transition is a possible follow-up. Covers the floating-nav rail too, since it opens sections the same way. Restore button and closing are unchanged, and desktop never auto-maximizes. Verified with a 50ms timeline on an emulated phone: maximized only after the scroll stopped, with the section at top 0 (~1.75s after the tap for the YouTube section), restore works, opening a second section maximizes it and closes the first.

**Side effect caught and fixed**: shadcn `PopoverContent`'s positioner was `z-50`, below the maximized section's `z-index: 2000`, so the Gemini Notebook popover opened *behind* a maximized YouTube section (confirmed via `elementFromPoint`). It's now `z-[2100]` in `src/components/ui/popover.tsx`. Any future overlay component (dialog, tooltip, dropdown) needs the same: above 2000.

## Status (2026-09-23 session — "Add to Gemini Notebook" button on the YouTube carousel)

**Every `VideoCard` in `YoutubeCarousel.tsx` now has an "Add to Gemini Notebook" button** (Gemini Notebook = NotebookLM's July 2026 rebrand), next to its Key Moments list. Went through a real `/grill-with-docs` session first (not a snap decision) - two rounds of live research kept overturning the design:

1. **No URL param exists** to prefill a NotebookLM source or chat prompt (confirmed via web search - the only real API is NotebookLM *Enterprise*, a paid GCP-service-account-gated product that creates notebooks in an org tenant, not the site owner's personal library; the Chrome extensions that look like one-click-add actually talk to NotebookLM's private backend through the extension's own authenticated session, not a link).
2. **Phones use the same flow as desktop (changed after first deploy)**. The first version sent phones through the OS share sheet (`navigator.share({title, text: prompt, url})`), since the NotebookLM mobile app registers as a share target. It was dropped for two reasons: whether the app keeps the shared prompt was never verified, and visitors without the app had no fallback (Gemini Notebook missing from the sheet, cancelling did nothing). Phones now copy link + prompt, get the popover ("long-press the chat box, tap Paste, then send"), and open the Gemini Notebook web app. Phones get the same 10s same-tab countdown; the popover wording there is "Start a new notebook if one isn't open, then long-press the chat box, tap Paste and send." The manual button (a real tapped link) still opens the app's home screen, which is why the wording covers that case.
3. **The open button is a real `<a href="https://notebook.google.com/new" target="_blank" rel="noopener noreferrer">`** styled with `buttonVariants`, not Base UI `Button render={<a/>}`, which stamps `role="button"` on the link and makes screen readers announce it as a button. It carries `data-slot="button"` by hand so the index.css Primer escape still applies. Clicking it closes the popover. `PopoverContent` now forwards Base UI `collisionPadding` (set to 16) so the popover keeps a 16px gutter on phones; the default was 5px.
4. **Desktop path**: click copies `video.url + "\n\n" + prompt` as **one paste** (user's explicit choice over a separate "Copy prompt" button), then shows a **shadcn Popover** ("✓ Link + prompt copied — in the new notebook, paste into the chat with [Ctrl+V]/[⌘V] and press Enter. Gemini adds the video and answers." — key cap via shadcn `Kbd`, key picked by `pasteKey`; touchscreens get "long-press the chat box, tap Paste, then send") with an **"Opening in 10s…"** countdown on the button's row that then loads **`https://notebook.google.com/new`** in the **same tab** (`location.assign`); the **Open Gemini Notebook** button opens it immediately, Escape/outside-click cancels. Popover spacing follows common conventions: 16px padding, 4px title gap, 12px to the action row, 320px wide. Why a countdown rather than opening at once: opening straight away stole focus before any "copied" feedback could be seen. **Why same-tab, and 10s (user's choice)**: measured in real Chrome with its popup blocker on (Playwright normally disables it — launch with `ignoreDefaultArgs: ["--disable-popup-blocking"]`): a script-opened *new tab* is **allowed up to 5s after the click, blocked at 6s+**, while a same-tab load is never blocked (verified at 10s, desktop + phone; back returns to the site). A code-triggered load also stays in the browser on phones: iOS treats `window.location` navigations as untrusted and won't open the app via Universal Links, and Chrome for Android only hands a navigation to an app if it comes from a user gesture (a synthetic one counts only within ~1s). That matters because, with the Gemini Notebook app installed, a *tapped* link opens the app on its **home screen** (tested on the user's phone), while the browser's `/new` creates a notebook. **The paste must go into the chat, not the source box** (tested live, signed in): a fresh `/new` notebook focuses the chat box; sending link + prompt there makes Gemini add the video as a source itself (~15–25s) and answer, while the "Website and YouTube URLs" source box rejects link + prompt (Insert stays disabled). **The prompt** (agreed in a `/grill-with-docs` session): `Video: "{title}" by {channel}. Explain it in plain, simple terms:` then 1) key takeaways as short bullets, 2) one or two practical ways to apply it, 3) three follow-up questions. The visitor already chose the video, so there's no "what is it / who is it for" overview. The follow-up questions help someone who doesn't know what they want from it. **Islamic Studies videos** (`category === "Islamic Studies"`, the curator's exact category name) get an extra instruction asking for each Quran verse and hadith to be cited *inside the point it supports*, with the Arabic text and an English translation (Saheeh International for the Quran; the translation shown on sunnah.com for hadith) (the user wants sources integrated into the answer, not segregated; a numbered "4." item made Gemini put them all in a separate section at the end, tested), with its exact number, a link (example formats `https://quran.com/2/255` and `https://sunnah.com/bukhari:1`, both verified to resolve: Ayatul Kursi and Sahih al-Bukhari 1; sunnah.com 403s `curl` via Cloudflare but loads in a browser), whether each hadith is sahih, and "say so instead of guessing" if a reference or its wording is unconfirmed. Tested in Gemini Notebook with the 4th-point version: it gave correct-looking numbers, sahih gradings and links, and flagged two references it couldn't link instead of inventing them. Gemini wraps links in google.com/url redirects; that's its UI, not the prompt. No "answer in English" (a Malay video still got an English answer), and no key moments or timestamp requests (only the transcript is imported, so timestamps could be invented; the notebook's own citations cover it). If the clipboard write fails, the popover says so and shows the link to add manually (no countdown). An `isActiveRef` re-check after the awaited write stops the popover opening on a card that swiped away mid-write. Prompt/clipboard/paste-key logic lives in `src/lib/geminiNotebook.ts` (+ `.test.ts`, 5 tests); the DOM/popover wiring itself is not unit-tested (verified by headless click tests instead: auto-open at ~4s, Escape cancels with no tab, blocked fallback, both themes).
5. **Why no URL pre-fill — verified in the real app, not just docs** (signed-in Playwright probe): `/new` is a server route that drops *every* query param on its redirect; the client's full query-param set (read via NgRx router selectors) is `addSource` (only opens the add-source dialog — value ignored, `!!a`), `src`/`artifactId` (notification analytics), `embed`/`present` (Studio artifact views), `skid` (tracking), `tst` (error toast). Routes: `notebook/creating` is a logic-free spinner, `import` is feature-flagged off. Only unknowable gap: other server-only routes like `/new`. (`notebooklm.google.com` redirects to notebook.google.com — observed.) Both paths verified with headless click tests: desktop — first click copies + shows the popover with no tab, Escape closes it, the Open button opens `/new` and closes it, both themes screenshotted; emulated phone — `navigator.share` called, no popover.

**Icon**: Simple Icons' NotebookLM mark inlined as an `inline-start` SVG — Simple Icons has no rebranded icon yet (`gemininotebook.svg` 404s), so swap it if one appears. **Dark mode fixed site-wide for shadcn**: `index.css`'s shadcn token block and `@custom-variant dark` were keyed to a `.dark` class that nothing sets (the site uses `data-theme="dark"` on `<html>`, via `useTheme.ts`), so every shadcn component rendered light-themed forever — both now key off `[data-theme="dark"]`. Verified with headless screenshots in both themes. **Primer vs shadcn cascade conflict also fixed**: Primer and `site.scss` are unlayered, and unlayered CSS beats Tailwind v4's `@layer utilities` whatever the specificity — Primer's `button { border-radius: 0; font: inherit }` was rendering shadcn buttons square-cornered, with inherited font size. **Layering Primer was rejected on purpose**: its utilities are nearly all `!important`, and `!important` inverts layer priority, so layered-Primer `!important` would beat every dark-mode `!important` override in `site.scss` (`.text-gray`, `.border`, …) and break contrast site-wide. Instead, `index.css` has `[data-slot][data-slot] { all: revert-layer }` — every shadcn part carries `data-slot`, so these elements discard all unlayered declarations and fall back to Tailwind's layers — plus `[data-slot]×3 { border-color: revert-layer !important }` for the one `!important` name collision (Primer's `.border` vs Tailwind's `border` utility). Inline `style` props still apply. Verified with computed styles (radius 8px, font 12.8px, `border-transparent` stays transparent) and screenshots in both themes; `npm run build` passes. **Future shadcn components get this automatically** — but a Primer spacing class (`px-3`, `mt-2`, …) put *directly on* a `data-slot` element will now be ignored; use Tailwind classes there. Renders via `@/components/ui/button` (shadcn/base-ui `Button`, `variant="outline" size="sm"`) per this session's instruction to prefer shadcn for UI components — this is that component's **first real usage anywhere in the site** (it already existed in `src/components/ui/button.tsx`, unused until now; confirmed the CSS custom properties it depends on - `--primary`, `--ring`, etc. - are already defined in `index.css` before adopting it, so it wasn't a blind first-use). The Popover (`src/components/ui/popover.tsx`) was added via `npx shadcn add popover`; the CLI mis-resolved the `cn` alias and installed an unrelated npm package named `cn` — import fixed to `@/lib/utils` and the package uninstalled, so watch for that on future `shadcn add` runs.

Also this session: wrote `docs/specs/0003-youtube-carousel-consent-autoplay.md` from an earlier `/grill-with-docs` session (autoplay-on-scroll/swipe for the carousel, consent-gated via the existing facade click, `localStorage`-persisted) — spec only, not yet implemented.

## Status (2026-09-21 session end — YouTube enrichment moved off Vertex AI)

**`fetch_youtube.js`'s video-enrichment step (`enrichWithVideoSummary`) no longer uses Vertex AI** — see `docs/specs/0002-agentic-video-enrichment.md` for the full spec (from a `/grilling` session) and rationale. That step was 96% of this pipeline's Gemini spend (RM63.92 of RM66.32/month), driven by brute-force full-frame + full-audio processing. It now runs on Gemini's newer Interactions API (`ai.interactions.create`, `processing: "agentic"`) via the same plain `GEMINI_API_KEY` `callGemini()` already used elsewhere in this file — confirmed working via live test calls (Python and JS SDK, both real API responses, not just docs) before implementing.

- **Auth fully dropped for this script**: the `GCP_PROJECT_ID`-gated skip-path is gone (enrichment now always runs when `GEMINI_API_KEY` is set, already required unconditionally). `ai-youtube.yml`'s `google-github-actions/auth@v2` step and `id-token: write` permission are removed — nothing in this script needs Vertex AI/Workload Identity Federation credentials anymore. The `GCP_WORKLOAD_IDENTITY_PROVIDER`/`GCP_PROJECT_ID` GitHub secrets are confirmed unused anywhere else in the repo; deleting the stored values in GitHub settings is a manual follow-up, not done here.
- **External contract unchanged** — same `{summary, timestamps, errors}` return shape, same retry/backoff loop (`VIDEO_ENRICHMENT_MAX_RETRIES`), same content-safety guardrails in the prompt (misinformation skepticism, no anti-Islamic content, no hallucinated timestamps, Shorts <60s), same reuse/dedup logic for already-enriched videos. Only the call inside the loop changed.
- **`@google/genai` bumped 2.18.0 → 2.23.0** (was locked stale despite `package.json` already saying `"latest"`) — the older version doesn't have the `Interactions` API at all, confirmed by inspecting its type definitions before assuming a bump alone would work.
- No fallback to the old Vertex path was kept (deliberate — direct cutover, already validated via live calls). The already-cheap selection/curation step (`evaluateBulk`) was explicitly left untouched — this migration targeted only the actual cost driver.

## Status (2026-09-21 session end — custom domain, firdauspuzi.com)

**The site now has a custom domain, `firdauspuzi.com`, live alongside the original `frdspuzi.github.io`** — see `docs/specs/0001-custom-domain-firdauspuzi-com.md` for the full spec (from a `/grilling` session) and rationale (professional branding for job search; GitHub Pages kept as host, no migration needed).

- **Infra side (already done, outside this repo):** DNS at the registrar (GoDaddy) points the apex at GitHub Pages' 4 IPs plus a `www` CNAME to `frdspuzi.github.io`; the repo's Settings → Pages → Custom domain is set to `firdauspuzi.com` (apex primary, `www` redirects into it), DNS check passed, HTTPS cert issuance is automatic/pending. No `CNAME` file needed in the repo — confirmed via GitHub's own docs that Actions-based Pages deploys don't use one.
- **Code side (this session):** `src/lib/legacyDomainRedirect.ts` — a pure `getLegacyDomainRedirectTarget(location)` function (hostname/pathname/search/hash in, target URL or `null` out) — plus its colocated `legacyDomainRedirect.test.ts` (this repo's first committed test, per the forward-only Testing Strategy in `architecture.md`). Wired into `main.tsx`: checked before `createRoot(...).render(...)`, so a visitor still on `frdspuzi.github.io` is sent to `firdauspuzi.com` (path/query/hash preserved) without React ever rendering the stale-URL page first. GitHub doesn't auto-redirect between a repo's default and custom domain — both would otherwise keep serving the same content in parallel forever.
- No changes to `_data/*.json`, content-generation workflows, or `deploy-pages.yml` — fully isolated from the automated content pipeline.

## Status (2026-08-23 session end, part 6 — top-5-plus-load-more per list, replacing matched-count capping)

**Each of the 2 carousel slides (GitHub, Product Hunt) now defaults to its own top 5 items with an independent "Load more"/"Show less" toggle**, instead of both being pre-capped to the same length via `matchedTrendingCount()`. That helper and its test (`src/lib/trendingCount.ts`/`.test.ts`) are deleted - no longer used anywhere.

- `GithubTrendingList.tsx`/`ProductHuntList.tsx` each own an `INITIAL_COUNT = 5` and local slicing (`expanded ? full : min(5, full)`), same shape `GithubTrendingList` used *before* the whole carousel rebuild. `expanded`/`onToggleExpanded` are now props, not local state - `TrendingCarousel.tsx` owns both flags (`githubExpanded`/`phExpanded`) so it can call its own `measureNow()` whenever either toggles (a `useLayoutEffect` keyed on both flags) - the offscreen measure copies and the visible track copies read the same state, so remeasuring after a toggle picks up the new item count correctly, verified via a real click-to-expand test (5 -> 18 GitHub cards, viewport height 1086px -> 3560px, zero clipping, zero console errors) and the reverse collapse (back to 1086px cleanly).
- **Entrance animation restored** on both card types - the `motion.div` (`initial: scale 0/opacity 0`, `whileInView`, spring transition, `delay: 1`) that `RepoCard` had *before* the 2026-08-23 carousel rebuild dropped it, brought back per explicit request ("keep previous animation") and now applied to `ProductCard` too for symmetry, both un-changed from the original values (including the `delay: 1` that was itself a prior session's own deliberate leftover, not a default worth re-tuning).
- `TrendingSection.tsx` simplified accordingly - no more `isDesktopWidthAtMount()`-gated count branching, just passes the full real lists straight through.

## Status (2026-08-23 session end, part 5 — replaces part 4's media[] screenshot with a small inline icon)

**Part 4's fix (below) was itself replaced the same day.** The full-width screenshot it introduced fixed the distortion, but a real height measurement showed it made Product Hunt cards ~4x a GitHub card's height (avg PH card 651px, screenshot alone 485px / 74.5% of that) — since the carousel deliberately shares one fixed height across both slides (so swiping never resizes it), that pushed a large empty whitespace gap onto the shorter GitHub slide, visible in the real rendered site.

- **Settled on: the square `thumbnail.url` logo, small (28×28) and inline next to the product name** — the exact treatment `GithubTrendingList.tsx`'s `RepoCard` already gives the repo owner's avatar, so `ProductCard`'s row 1 now mirrors `RepoCard`'s row 1 structure directly. `rounded-2` not `rounded-full` (these are square app icons, not person avatars — a circle crop looks wrong on most of them).
- The `media[]`/`thumbnailIsLogo` logic from part 4 is gone entirely — no longer needed once the square logo became the *intended* shape instead of a fallback to route around. `fetch_producthunt.js` only queries `thumbnail { url }` again; `ProductHuntPost` has a single `iconUrl: string` field.
- Measured result: panel-height gap dropped from the original ~8,758px down to roughly balanced (a small real sample measured GitHub 996px vs. Product Hunt 764px for 5 matched items each) — no more visible whitespace problem.
- Tests in `fetch_producthunt.test.js` simplified back down to match — just `iconUrl` extraction, no media-preference/fallback cases to cover anymore.

## Status (2026-08-23 session end, part 4 — introduced, then replaced by part 5, a media[]-based screenshot)

**Product Hunt cards were rendering badly distorted images** — caught by looking at the real rendered site (screenshot showed cropped, zoomed-in icon fragments instead of app screenshots), not assumed. Root cause, confirmed via real API investigation: `fetch_producthunt.js` was requesting `thumbnail { url }`, which Product Hunt's API always returns as a **square logo/icon** (confirmed 1:1 dimensions on every sample checked) — rendering that at `aspectRatio: "16/9"` with `objectFit: "cover"` stretched and cropped it into the distorted result.

- **Fix**: query `media { type url }` too. Every one of 20 real sampled posts had at least one `type: "image"` entry, and every one checked was landscape (several exactly 1200×630, the OG-image ratio) — real launch screenshots, not logos. `parsePost()` now prefers the first `type: "image"` media entry, falling back to `thumbnail.url` only if a post genuinely has none (not observed in the real sample, but kept as a safety net).
- **`ProductHuntPost` gained `thumbnailIsLogo: boolean`** so the frontend knows which shape it got. `ProductCard` (`ProductHuntList.tsx`) renders a real screenshot full-width at 16/9 as before, but a logo fallback renders small and square (48×48, matching GitHub's owner-avatar treatment) instead of being stretched.
- Re-ran `fetch_producthunt.js` for real after the fix — all 20 current posts got real screenshot URLs, zero logo fallbacks in practice today.
- Tests in `fetch_producthunt.test.js` now cover: media preferred over thumbnail, video-type media entries skipped, fallback to thumbnail (with `thumbnailIsLogo: true`) when media has no image entries or is absent entirely.

## Status (2026-08-23 session end, part 3 — corrects part 2's carousel shape)

**Part 2 (below) built the wrong shape for the GitHub/Product Hunt swipe carousel** — two separate carousels, each swiping through individual repos/products one at a time. What was actually asked for, caught only once the user looked at localhost: **one carousel, exactly 2 fixed slides — slide 1 is the full GitHub list, slide 2 is the full Product Hunt list, and swiping switches which whole source you're looking at**, not which individual item.

- **`TrendingCarousel.tsx`** (new) owns the swipe physics now — same imperative-refs shape as `YoutubeCarousel.tsx`, but with exactly 2 fixed positions (`SLIDE_IDS = ["github", "producthunt"]`) instead of N items. `GithubTrendingCarousel.tsx`/`ProductHuntCarousel.tsx` (part 2's per-item carousels) are deleted.
- **`GithubTrendingList.tsx`/`ProductHuntList.tsx`** (new — note `GithubTrendingList.tsx` reuses a filename from *before* part 2 ever existed, now meaning something different again: a plain vertical list of cards, not individually swipeable) are the 2 slides' content. Each renders its own heading at the top ("Trending on GitHub · weekly" / "Launched on Product Hunt · daily") so the label swipes with its content instead of living outside the carousel as a static subheading.
- **Item counts, corrected reasoning**: both lists are capped to the *same* length on **both** mobile and desktop now (`matchedTrendingCount()`, unchanged from part 2) — not just desktop as part 2 said — because the reason for matching counts is specifically so swiping between the 2 slides never resizes the carousel, which matters at every viewport, not only desktop. Mobile additionally caps to `MOBILE_COUNT = 5` on top of that match (`Math.min(MOBILE_COUNT, matched)`), since a phone screen can't usefully show as many items as desktop regardless of how the two sources compare.
- 2-dot pagination added (reuses `TriviaBoard.tsx`'s own `.trivia-dot`/`.trivia-dot.active` CSS, React-rendered here since `currentIndex` is already real state in this carousel, unlike `TriviaBoard`'s fully-imperative one) so it's visually obvious this is a 2-page swipe, not an open-ended list.
- `draggable={false}` invariant (architecture.md's Component Conventions) still applies unchanged — every card's `<a>` and `<img>` still needs it, now inside the list panels instead of per-item carousel slides, same underlying reason.
- Verified via real headless-browser tests (same discipline as before): exactly 1 carousel viewport in the DOM (not 2), 2 dots, a real drag swipe transitions from the GitHub panel to the Product Hunt panel, and card counts on both slides match when Product Hunt has fewer items than GitHub.

## Status (2026-08-22 session end, part 2 — corrected 2026-08-23, see part 3)

**"What the Internet's Building" now covers Product Hunt too, not just GitHub trending — and both sources present inside ONE swipe carousel with exactly 2 slides, instead of the old grid + "Load more" button.** Decided via a second `/grill-with-docs` session. **Bullets 3, 5, and 6 below describe an earlier, wrong shape (two separate per-item carousels, and mobile-only item-count matching) that was corrected the next day — see part 3 for what actually shipped; the rest of this entry is still accurate.**

- **New data source**: `.github/scripts/fetch_producthunt.js` + `.github/workflows/producthunt-trending.yml` (daily cron, 03:00 UTC, also added to `deploy-pages.yml`'s `workflow_run` trigger list) + `_data/producthunt.json` (seeded empty until the workflow's first real run). Uses Product Hunt's GraphQL API (`api.producthunt.com/v2`) with a static Developer Token (`PRODUCTHUNT_API_TOKEN` secret, already set up on the GitHub repo) — **not** a scrape, unlike `fetch_github.js`. See architecture.md's Data Flow section for why (Cloudflare bot-gate + an explicit ToS scraping prohibition, confirmed via direct investigation, not assumed).
- **Daily, not weekly**, for Product Hunt specifically — GitHub trending stays weekly, unchanged. Confirmed via a real week of live Product Hunt data that weekly would structurally discard content (a product launches once and never reappears, so a week's `weeklyRank` list only ever reflects 3-4 of that week's 7 days) — this is a different situation from GitHub trending's own weekly choice, which is about smoothing noise over repos that *do* reappear day to day.
- **Both GitHub and Product Hunt are now swipe carousels** (`GithubTrendingCarousel.tsx`, `ProductHuntCarousel.tsx`), replacing the old `GithubTrendingList.tsx` grid-of-cards + expand/collapse button UI (deleted). Structurally modeled on `YoutubeCarousel.tsx` (state-driven `currentIndex`, keyed prev/current/next window, imperative-refs-only swipe physics) rather than `TriviaBoard.tsx`'s fully-imperative approach, since repo/product cards have neither a video player lifecycle nor a reveal-answer state machine. Kept as two separate self-contained files per this codebase's existing carousel convention, not a shared hook.
- **Real bug found via headless-browser testing, now a documented invariant** (see architecture.md's Component Conventions): wrapping a card's whole draggable surface in `<a href>` (and separately, any `<img>` inside it) needs `draggable={false}` on *each* such element, or the browser hijacks a mousedown-then-move into a native link/image drag instead of continuing to fire the carousel's own mousemove/mouseup handlers — confirmed via a real drag simulation that showed `startSwipe` firing, then nothing, `endSwipe` never called. Two separate instances of the same bug class in one session (the card's own `<a>`, then `ProductCard`'s thumbnail `<img>` needed the identical fix independently) — worth remembering `draggable` doesn't inherit.
- `TrendingSection.tsx` (renamed from `GithubTrending.tsx`, same `id="github-trending"` kept unchanged to avoid breaking any existing deep link) now houses both carousels stacked under one accordion, with a small subheading above each ("Trending on GitHub · weekly" / "Launched on Product Hunt · daily") so the two sources stay legible.
- **Item counts**: mobile always caps both lists to top 5; desktop caps both to whichever source currently has *fewer* items (`matchedTrendingCount()` in `src/lib/trendingCount.ts`, with a fallback to the non-zero side so an empty Product Hunt feed doesn't hide a perfectly good GitHub list) — decided once at mount, matching the existing `isDesktopWidthAtMount()` convention rather than adding new reactive resize-tracking.
- Section broadened its actual meaning (per an explicit "yes, genuinely broadening" confirmation, not just "surface PH data somewhere") — intro copy updated to mention both sources, `FloatingNav.tsx`'s rail-preview description updated too.
- **`_data/producthunt.json` is currently seeded empty** (`{ "posts": [], "lastUpdated": null }`) — the workflow hasn't run for real yet. First real run populates it; until then the Product Hunt half of the section simply doesn't render (GitHub's half still does).
- Tests: `.github/scripts/fetch_producthunt.test.js` (mirrors `fetch_github.test.js`'s pattern) + `component-lab/src/lib/trendingCount.test.ts` for the count-matching logic, per the testing policy adopted earlier this session. Deliberately did **not** add a Playwright spec for the new carousels — the policy explicitly reserves E2E for flows that have "actually bitten twice, not written speculatively," and this is brand new with no history yet.

## Status (2026-08-22 session end, part 1)

**Added a testing toolchain to component-lab — first automated tests of any kind in this sub-project.** Decided via a `/grill-with-docs` session after the whole 2026-08-20 GithubTrending build relied entirely on ad hoc, written-then-deleted headless-browser scripts for verification. Full policy is in architecture.md's new "Testing Strategy" section — summary: Vitest + React Testing Library for component/logic tests, Playwright for critical cross-cutting E2E flows, forward-only (nothing retroactively backfilled for existing features), no enforced coverage percentage, no visual regression tooling. Wired into a local pre-commit hook (Husky + lint-staged + typecheck + Vitest). **CI wiring (an `npm test` step in `deploy-pages.yml`) is not yet done** — pending explicit user confirmation since it's a workflow-file change.

Two real bugs caught during setup, worth knowing if the test config gets touched again: (1) Vitest's default include glob matches `*.spec.ts` regardless of directory, so it tried to run `e2e/smoke.spec.ts` (a Playwright spec) and failed with "Playwright Test did not expect test() to be called here" — fixed by extending `vite.config.ts`'s `test.exclude` to cover `**/e2e/**` (had to spell out Vitest's own default excludes too, since setting `exclude` replaces rather than extends them). (2) `.husky/pre-commit` initially had no `&&`/`set -e`, so in POSIX `sh` every line ran regardless of an earlier failure and the hook's exit code was just whatever the *last* line returned — a failing lint or typecheck step wouldn't actually have blocked a commit. Fixed by adding `set -e` as the first real line.

Also: `npx husky init` / bare `npx husky` fail with `.git can't be found` in this environment (reproduced in both Git Bash and PowerShell) and don't set `core.hooksPath` — worked around by setting it manually (`git config core.hooksPath component-lab/.husky`). See architecture.md's Testing Strategy section for the full note; if hooks stop firing after a fresh clone, check that config value first.

## Status (2026-08-20 session end)

**Added a 4th groupable homepage section: "What the Internet's Building"** (`GithubTrending.tsx` + `GithubTrendingList.tsx`), showing a weekly-refreshed, uncurated list of GitHub's trending repos with AI-written plain-language (ASD-STE100) hooks and an optional one-line personalization note. Backend: new `.github/scripts/fetch_github.js` (real-HTML regex scrape of `github.com/trending?since=weekly`, no third-party API — same "single, consistent, machine-generated format" justification as the YouTube Atom feed parser), new weekly-cron `.github/workflows/github-trending.yml`, writes `_data/trending.json`. Rendering uses MagicUI's real `AnimatedList` (first `motion`-based magicui component in this app — installed to `src/components/ui/animated-list.tsx` via the shadcn CLI against the raw magicui registry URL, matching architecture.md's component-sourcing rule).

**Real bug found and fixed via headless-Chrome verification, worth knowing if `GithubTrending.tsx` gets touched again**: `AnimatedList`'s incremental item-reveal (children mount one at a time via internal `setTimeout`) races `useAnimatedDisclosure`'s one-shot `scrollHeight` measurement at open-start — mounting the list immediately in `onBeforeMeasure` (matching `Photography.tsx`'s literal pattern) locks the accordion to a stale, one-card-tall height with `overflow:hidden` while more cards silently mount underneath, then all "pop" into view at once when `settle()` releases the lock at `OPEN_DURATION` (580ms). This is architecture.md's invariant #3/#4 bug class ("content growing post-measurement inside an Accordion"), just via a new source (a third-party animated list) rather than the previously-documented ones. Only reachable via a real open transition (a click) — `useAnimatedDisclosure`'s `isFirstRun` path (desktop `defaultOpen`-from-first-paint) never runs the WAAPI animation at all, so a naive test that only checks the desktop-default-open state won't catch this. Fixed by delaying the list's mount (via `onBeforeMeasure`) until 650ms after a real transition starts — past `OPEN_DURATION`, no changes to shared `Accordion`/`useAnimatedDisclosure` needed. Verified via a real headless-browser test sampling the content element's computed `overflow` + rendered card count every 100ms through a real click-to-open on a mobile viewport: confirmed zero cards ever render while `overflow: hidden` still holds.

**`PROFILE.md`** (repo root, gitignored, dev-time-only reference — see `docs/adr/0001-manual-profile-for-content-personalization.md`) is now the source both this section's and `fetch_youtube.js`'s personalization prompts should be hand-distilled from. Category-level only in committed code — no specific project names/URLs/employer details, see the ADR for why (the generated output is public even when the prompt input wouldn't be).

## Status (2026-08-13 session end)

**Added a mobile-only "maximize" toggle to the 3 groupable accordion sections** (YouTube feed, Insights & Writing, Photography) — a true fullscreen takeover (`position: fixed; inset: 0`), not just chrome-trimming, per explicit user framing ("like how you maximise a window"). Lives in `Accordion.tsx` (local `isMaximized` state, un-maximizes automatically if the section closes out from under it) and `site.scss`'s `.animated-details.is-maximized` block, gated to `@media (max-width: 767px)`. Button icons are `lucide-react`'s `Maximize2`/`Minimize2` (checked beui.dev's registry first per the component-sourcing rule — nothing there fit, hand-built confirmed appropriate).

Two real bugs only showed up under headless-Chrome verification, both worth knowing if this block gets touched again: (1) the header title didn't actually hide — `.accordion-header-title` also carries Primer's `.d-flex`, which is `display: flex !important`, so the maximize rule's plain `display: none` lost regardless of its higher selector specificity; fixed by matching `!important`. (2) the "fullscreen" overlay showed the masthead and other page content bleeding through at full opacity — `--overlay-bg` (the card's normal background, set via inline style in `Accordion.tsx`) is a near-transparent tint (`rgba(...,0.01)`), intentionally subtle for an in-flow card sitting on the page background, but wrong once that same element is blown up to fill the viewport; fixed by overriding to `var(--surface-page)` (opaque) with `!important` (inline styles beat non-important class rules). Verified all 3 sections individually via `chrome-launcher`/`puppeteer-core` at a 430×900 mobile viewport: fixed positioning, z-index above the floating nav rail's 1000, opaque background, title/chevron hidden, content fills available height, restore-to-normal all confirmed working, zero console errors.

## Status (2026-08-12 session end)

**Deployed to production this session.** Merged `feat/component-lab` into `master`, built the `deploy-pages.yml` workflow (with the standard GitHub-Pages-SPA-routing 404.html trick, since this app uses `BrowserRouter`), and — after several real bugs surfaced only once it was actually live (a squished Jekyll front-matter line that had silently broken the *old* site's CSS for a while, an accidental content revert in a follow-up fix commit, `[skip ci]` on every automated content commit meaning the deploy workflow never fired after a content refresh, and stale committed image copies in `public/assets/` going out of sync with the root `assets/` folders) — confirmed working end-to-end via a real headless-Chrome-driven verification (`chrome-launcher` + `puppeteer-core`, both already in `node_modules` from the Lighthouse tooling). See git log around this date for the full bug chain and fixes; the short version is captured in the "Still shared" note above and the deploy workflow's own comments.

## Status (2026-08-07 session end)

**Every item in the original build plan is now built and committed:** app shell, the shared accordion system, all 5 homepage sections (Masthead, Gratitude, YouTube feed, Insights & Writing, Photography), the floating Preview Rail nav, a full data-wiring pass, and the blog post route/layout. Scope was "full site port, eventual goal of replacing Jekyll as the live deploy" — deployment cutover itself is still explicitly deferred (see architecture.md's "Not Yet Decided" section) and hasn't started.

**Section notes worth knowing before touching each one:**
1. App shell — `react-router-dom` (home + `/:year/:month/:day/:title/` post route matching `_config.yml`'s permalink), Primer CSS + site.scss import, theme toggle (`data-theme` attribute, **dark by default** now — see below), `Layout.tsx`.
2. Shared accordion system — `Accordion.tsx` / `useAccordionGroup.tsx` / `useAnimatedDisclosure.ts`. Read architecture.md's hard-won invariants before touching any of these three files; the bug chain that produced them is expensive to rediscover.
3. Masthead — avatar, name/bio, location, GitHub/LinkedIn social icons. Gained a `metadata` prop (default `true`) so `Post.tsx` can render it with `metadata={false}` (avatar/bio only, no contact-info/social-icons block). Also gained a full-bleed animated shader background (`ShaderBackground`, real `@beui` component) behind the hero, with a fixed-color scrim + fixed hero text/icon colors (`SCRIM_COLOR`/`HERO_TEXT_COLOR`/etc.) since the shader doesn't respond to the theme toggle the way the surface it replaced did.
4. `AboutContent.tsx` — ports `index.html`'s own markdown body (rendered via Jekyll's `{{ content }}` in `home.html`, a separate template context from `masthead.html`) — this was missed in the initial port and added later after the user noticed it was gone.
5. Gratitude journal — textarea + AI-suggestion typewriter effect, own (non-groupable) disclosure that still participates in mobile single-open.
6. YouTube feed ("Reclaiming the Algo") — swipe carousel. **Rewritten 2026-08-07** around a single `VideoCard` component rendered 3x (prev/current/next), keyed by video id, instead of one real imperative "current" card plus a separate raw-HTML-string preview builder for the peek slides — the old two-path design was the root cause of a real "not seamless" swap discrepancy that CSS-parity patching couldn't fully close. See this file's Recent Work section and the file's own top-of-file comment before changing it again; the `useLayoutEffect`-keyed-on-`[currentIndex]` transform-reset sequencing is load-bearing, not incidental.
7. Insights & Writing — Medium card tray (`MediumTray.tsx`, real JSX cards) + AI trivia swipe carousel (`TriviaBoard.tsx`, still the older imperative-refs pattern the YouTube carousel used to use — not yet revisited with the same VideoCard-style refactor; a reasonable candidate if the same "preview vs real" drift ever gets reported there too).
8. Photography — `PhotographyCanvas.tsx`, a close-to-1:1 port of the Canvas 2D Infinite Image Field (drag-to-pan with momentum, bento tile layout, `IntersectionObserver`-deferred image loading, skeleton pulse). 41 photos copied into `public/assets/photography/`.
9. Floating Preview Rail nav — `FloatingNav.tsx` wraps the real `@beui/preview-rail` (installed via shadcn, then hand-edited to use this site's own CSS custom properties instead of shadcn's `.dark`-class theme tokens, which nothing on this site ever sets). Scroll-spy + `useAccordionGroup` integration for opening a closed section before scrolling to it.
10. Blog post route (`Post.tsx`) — `_posts/*.md` imported via Vite's `?raw`, rendered with `marked` (new, justified dependency — no client-side Kramdown equivalent). Code blocks get a plain `.highlight` wrapper, not full Rouge-parity syntax tokenization (deliberate scope cut — the one post on the site is an unpublished placeholder).

## Recent Work

**YouTube carousel VideoCard refactor (2026-08-07).** After fixing one real bug in the old prev/current/next architecture (a `p-4` class's `!important` padding silently beating a non-important React inline style, making the "current" card ~12px shorter than the preview slides for the same video), the user reported the swap still wasn't quite seamless — text/badges shifting position right at the moment a preview became current. Further static-analysis debugging didn't turn up a second discrete bug. The user proposed rendering prev/next as the same real card component instead of a simplified preview — recognized as the architecturally correct fix (it doesn't patch the discrepancy, it removes the only place it could come from) and implemented: one `VideoCard` component, rendered 3x and keyed by `video.videoId`, relying on React's keyed-list reconciliation to move/preserve each card's state as the 3-video window slides on swipe. Peek (non-active) cards render identical markup to the active card but are inert — no click/keyboard handlers wired, `tabIndex={-1}` — per explicit user confirmation ("inert until active"). `currentIndex` is now real `useState`; the track's instant transform-reset on commit moved from inline-in-the-swipe-`setTimeout` into its own `useLayoutEffect` keyed on `[currentIndex]`, so it's guaranteed to run only after React has already re-rendered the new video into the center slot (doing the reset first, before the state update flushes, would show the old video freshly recentered for a frame before flashing to the new one). Also added a shared module-scoped YouTube IFrame API loader, since API access is no longer tied to one hardcoded card.

**A run of visual bug reports (2026-08-06/07)**, each investigated via CSS/spec reasoning rather than a visual tool (no browser access this environment — see Verification notes) and fixed:
- Avatar/map-pin misalignment — Tailwind Preflight forces `img`/`svg` to `display:block`, breaking Primer's inline-flow/`text-align:center` assumptions. Fixed with `margin:0 auto` (img) / explicit `display:inline-block` inline style (svg — inline style beats a plain element-selector rule regardless of import order).
- YouTube facade thumbnail invisible — `.yt-facade`/`.yt-facade-thumb`/`.yt-facade-play` were never actually centralized into `site.scss`, only present in the original's own `<style>` block; an earlier grep match on the other two terms wrongly suggested full coverage.
- Medium articles horizontal-scrolling — `#medium-posts` (a flex item) had no `min-width:0` (flex default is `min-width:auto`), plus `overflow-y:auto` with no explicit `overflow-x` computing to `overflow-x:auto` per spec.
- Added a full-bleed animated shader background to Masthead's hero (`position:absolute; left:50%; width:100vw; transform:translateX(-50%)` breakout pattern, `body{overflow-x:hidden}` needed since 100vw includes scrollbar width) — required fixed (non-theme-reactive) text/icon colors since the shader doesn't respond to the toggle the way the surface it replaced did.
- Aurora-text gradient loop seam — `animation:infinite` snaps from the last keyframe color straight back to the first on every loop; fixed by repeating the first color as a final gradient stop (the user has since further hand-tuned the palette directly).
- Dark-mode-by-default — `index.html`'s `data-theme` and `useTheme.ts`'s SSR-guard fallback both flipped from `light` to `dark`.
- Theme-toggle view-transition not covering short pages — `.min-height-full`'s `min-height:100%` had no definite-height ancestor to resolve against (`<html>`'s own height is auto/content-sized by default), which also silently broke the View Transition API's root-snapshot sizing. Fixed with `html{height:100%}`.
- Missing About section — `index.html`'s own markdown body was never ported at all (rendered via a separate Jekyll template context from `masthead.html`, easy to miss). Built as `AboutContent.tsx` after the user asked "where's my about section?"

**The accordion animation bug chain (2026-08-06)** — read architecture.md's invariants before touching this system again; that section is the narrative behind them. Root cause: `open` state used to be seeded by a mount-effect calling `toggle()` a render after the accordion's own first render, so every `defaultOpen` accordion's `open` lagged `true` by one render, cascading into a mobile force-close race, a stale-animation-clobbers-the-fix bug, and (from an interim attempted patch) a reactive-`display`-races-imperative-WAAPI-code regression. Real fix: `isOpen`/`toggle` now take the caller's own `defaultOpen` prop directly as a fallback, correct from render 1; `useAnimatedDisclosure` became sole owner of `display`, switched to `useLayoutEffect`.

**Component-sourcing rule**, established mid-session and applied to every section since: check beui/magicui/aceternity/shadcn registries first, verify actual source (not just name) before concluding fit. Confirmed real matches: `@beui/preview-rail` (the floating nav). Confirmed non-fits, ruled out after reading actual source: `bouncy-accordion` (single-open-only, doesn't fit 3 independent multi-open-on-desktop sections), `@beui/infinite-masonry` (virtualized vertical list, not a 2D drag-pan canvas).

## Pending / not yet done
- **Live visual verification of everything built this session.** No browser/screenshot tool has been available in this environment at any point — every change across every session has been verified only via `npm run build` (clean TypeScript + Vite build) and the dev server responding to a `curl`, never an actual look. Several real bugs in this project (the whole accordion animation chain, the facade CSS gap, the YouTube carousel padding mismatch) were only ever found because the user was looking at a real browser and reporting back — treat nothing above as visually confirmed until that happens.
- `feat/component-lab` has never been pushed to origin — it's local-only.
- Root `CLAUDE.md`/`.ai/architecture.md` cross-branch reference edits (documenting that this branch exists) were proposed at one point but never explicitly confirmed or cherry-picked onto `master` — low priority, unresolved.
- `TriviaBoard.tsx` still uses the older imperative-refs-only carousel pattern the YouTube carousel used to use, not the newer VideoCard-style real-component-everywhere pattern — worth revisiting if a similar preview/real drift ever gets reported there.
